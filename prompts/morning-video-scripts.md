7AM VIDEO SCRIPTS — HARD MODE (ACTUALLY INSERT)

Run the real Video Bot script which generates today’s scripts and inserts them into Supabase tasks (so Mission Control Content updates):

bash /Users/henryburton/.openclaw/workspace-anthropic/scripts/video-bot/run-video-bot.sh

Then post a short summary notification into Supabase notifications (type=system) so Josh sees it in Mission Control.

Rules recap:
- 4 TikToks daily
- 2 YouTube only Monday and Thursday
- No dashes/hyphens
- South African tone
- created_by must be 'Video Bot'
- tags must include 'tiktok' or 'youtube' plus 'content'

Delivery: none (no announce spam).